syntax = "proto3";

package api;

message TracksInfo {
    string name = 1;
    string artist = 2;
}

message LyricsInfo {
    string lyrics = 1;
    string geniusURI = 2;
}

service Lyrics {
    rpc GetLyrics(TracksInfo) returns (LyricsInfo) {}
}
